"use client";
import {useEffect,useState} from "react";
import Link from "next/link";
import {AppShell} from "@/components/app-shell";
export default function AuditPage(){const[logs,setLogs]=useState<any[]>([]);const[m,setM]=useState("");useEffect(()=>{fetch("/api/audit").then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setLogs(d.logs??[])}).catch(e=>setM(e.message??"Audit se nepodařilo načíst."));},[]);return <AppShell><div className="content"><header className="page-header"><div><p className="eyebrow">Správa</p><h1 className="page-title">Auditní log</h1><p className="page-subtitle">Historie důležitých změn v účtu firmy.</p></div><Link className="button button-secondary" href="/nastaveni">← Nastavení</Link></header>{m&&<div className="auth-error settings-message">{m}</div>}<section className="panel"><div className="table-wrap"><table className="data-table"><thead><tr><th>Datum</th><th>Akce</th><th>Entita</th><th>ID</th><th>Detail</th></tr></thead><tbody>{logs.length?logs.map(x=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString("cs-CZ")}</td><td>{x.action}</td><td>{x.entity}</td><td>{x.entityId??"-"}</td><td>{x.details??"-"}</td></tr>):<tr><td colSpan={5} className="table-muted">Auditní log je zatím prázdný.</td></tr>}</tbody></table></div></section></div></AppShell>}
